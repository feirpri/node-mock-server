{
  "name": "easyci-mock-server",
  "version": "1.0.0",
  "description": "easyCI mock数据服务器",
  "main": "app.js",
  "scripts": {
    "start": "forever start -w -a -l forever.log -o output.log -e error.log ./bin/www",
    "restart": "forever restart -w -a -l forever.log -o output.log -e error.log ./bin/www",
    "stop": "forever stop ./bin/www",
    "addapi": "node ./bin/addapi",
    "removeapi": "node ./bin/removeapi",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "舍掉英熊",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.4.1",
    "body-parser": "^1.15.2",
    "debug": "^2.2.0",
    "express": "^4.14.0",
    "express-handlebars": "^3.0.0",
    "express-session": "^1.13.0",
    "express-xml-bodyparser": "^0.3.0",
    "moment": "^2.14.1",
    "superagent": "^2.0.0"
  },
  "devDependencies": {
    "colors": "^1.1.2",
    "forever": "^0.15.2",
    "open": "0.0.5"
  }
}